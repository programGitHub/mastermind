export enum Color {
  Black,
  Blue,
  Brown,
  Green,
  Orange,
  Red,
  Void,
  White,
  Yellow,
}

export type CodeColors = [Color, Color, Color, Color, Color];

export type Correction = [number, number];
